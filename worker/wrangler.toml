name = "remove-bg-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
# REMOVE_BG_API_KEY should be set in Cloudflare dashboard or .dev.vars

# Enable Node.js compatibility for some npm packages (if needed)
compatibility_flags = ["nodejs_compat"]
